# 📋 دستورالعمل تست - گام به گام

## ✅ مراحل انجام شده:
- ✅ وابستگی‌ها نصب شدند
- ✅ کتابخانه از حالت توسعه نصب شد
- ✅ Migration اجرا شد

## 🔧 مرحله 1: وارد کردن کدهای درگاه

**فایل `sep_test/settings.py` را باز کنید** و خطوط زیر را پیدا کنید:

```python
"SEP": {
    "MERCHANT_CODE": "YOUR_MERCHANT_CODE",  # خط 139
    "TERMINAL_CODE": "YOUR_TERMINAL_CODE",  # خط 140
},
```

**کدهای واقعی خود را جایگزین کنید:**
```python
"SEP": {
    "MERCHANT_CODE": "1234567890",  # کد پذیرنده واقعی
    "TERMINAL_CODE": "12345678",     # کد ترمینال واقعی
},
```

## 🚀 مرحله 2: اجرای سرور

در ترمینال، دستور زیر را اجرا کنید:

```bash
cd test_project
source venv/bin/activate
python manage.py runserver
```

یا اگر از محیط مجازی استفاده نمی‌کنید:

```bash
cd test_project
python manage.py runserver
```

خروجی باید چیزی شبیه این باشد:
```
Starting development server at http://127.0.0.1:8000/
Quit the server with CONTROL-C.
```

## 🌐 مرحله 3: باز کردن در مرورگر

1. مرورگر خود را باز کنید
2. به آدرس زیر بروید:
   ```
   http://127.0.0.1:8000/
   ```

3. باید صفحه اصلی تست را ببینید که شامل:
   - راهنمای استفاده
   - دکمه "شروع تست پرداخت"

## 💳 مرحله 4: تست پرداخت

1. روی دکمه **"شروع تست پرداخت"** کلیک کنید
2. در فرم پرداخت:
   - **مبلغ** را وارد کنید (حداقل 1000 ریال)
   - **شماره موبایل** را وارد کنید (اختیاری)
3. روی دکمه **"ارسال به درگاه بانک سامان"** کلیک کنید

## ✅ مرحله 5: بررسی نتیجه

### اگر موفق بودید ✅:
- باید به **صفحه درگاه بانک سامان** هدایت شوید
- صفحه پرداخت بانک سامان نمایش داده می‌شود
- این یعنی تغییرات شما **موفق** بوده است! 🎉

### اگر خطا دیدید ❌:

#### خطای "ترمینال به این نسخه از درگاه دسترسی ندارد":
- ✅ این خطا باید با تغییرات ما برطرف شده باشد
- اگر هنوز می‌بینید:
  1. بررسی کنید که کدهای پذیرنده و ترمینال درست وارد شده باشند
  2. بررسی کنید که URL در `azbankgateways/banks/sep.py` به `onlinepg/onlinepg` تغییر کرده باشد

#### خطای "آدرس ارجاعدهنده معتبر نیست":
- دامنه در Django Sites را بررسی کنید
- برای localhost می‌توانید این مرحله را رد کنید

#### خطای "آدرس سرور پذیرنده معتبر نمی‌باشد":
- IP سرور را بررسی کنید
- IP باید در لیست IP های مجاز بانک سامان باشد

## 🔍 بررسی لاگ‌ها

اگر مشکلی پیش آمد، لاگ‌های Django را بررسی کنید. در ترمینالی که سرور را اجرا کرده‌اید، پیام‌های خطا نمایش داده می‌شوند.

## 📝 نکات مهم:

1. **کدهای درگاه**: حتماً کدهای واقعی را وارد کنید
2. **دامنه**: برای تست localhost، نیازی به تنظیم دامنه نیست
3. **HTTPS**: در محیط production از HTTPS استفاده کنید
4. **IP**: اگر از سرور استفاده می‌کنید، IP باید مجاز باشد

## 🎯 نتیجه مورد انتظار:

اگر همه چیز درست باشد:
- ✅ به صفحه درگاه بانک سامان هدایت می‌شوید
- ✅ صفحه پرداخت نمایش داده می‌شود
- ✅ می‌توانید پرداخت را انجام دهید (یا لغو کنید)
- ✅ پس از بازگشت، نتیجه را می‌بینید

## 🆘 در صورت مشکل:

1. لاگ‌های ترمینال را بررسی کنید
2. بررسی کنید که کدهای درگاه درست وارد شده باشند
3. بررسی کنید که کتابخانه از حالت توسعه نصب شده باشد (`pip install -e ../`)
4. Issue #158 را در GitHub بررسی کنید

---

**موفق باشید! 🚀**


