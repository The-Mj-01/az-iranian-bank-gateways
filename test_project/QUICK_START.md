# راهنمای سریع تست

## 🚀 شروع سریع

### 1. تنظیم اطلاعات درگاه

فایل `sep_test/settings.py` را باز کنید و خطوط زیر را پیدا کنید:

```python
"SEP": {
    "MERCHANT_CODE": "YOUR_MERCHANT_CODE",  # کد پذیرنده خود را وارد کنید
    "TERMINAL_CODE": "YOUR_TERMINAL_CODE",  # کد ترمینال خود را وارد کنید
},
```

### 2. نصب وابستگی‌ها

```bash
cd test_project
pip install -r requirements.txt
```

### 3. اجرای Migration

```bash
python manage.py migrate
```

### 4. تنظیم دامنه (اختیاری برای localhost)

اگر از localhost استفاده می‌کنید، می‌توانید این مرحله را رد کنید.
در غیر این صورت:
1. `python manage.py createsuperuser` (برای دسترسی به ادمین)
2. به `http://127.0.0.1:8000/admin/` بروید
3. Sites → Site با ID=1 را ویرایش کنید
4. Domain name را تنظیم کنید

### 5. اجرای سرور

```bash
python manage.py runserver
```

### 6. تست

1. به آدرس `http://127.0.0.1:8000/` بروید
2. روی "شروع تست پرداخت" کلیک کنید
3. مبلغ و شماره موبایل را وارد کنید
4. به درگاه بانک سامان هدایت می‌شوید

## ✅ بررسی موفقیت

اگر به درگاه بانک سامان هدایت شدید و صفحه پرداخت نمایش داده شد، یعنی تغییرات موفق بوده است! 🎉

## ❌ در صورت خطا

اگر خطای "ترمینال به این نسخه از درگاه دسترسی ندارد" دیدید:
- بررسی کنید که کدهای پذیرنده و ترمینال درست وارد شده باشند
- بررسی کنید که URL در `sep.py` به `onlinepg/onlinepg` تغییر کرده باشد
