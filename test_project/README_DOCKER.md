# 🐳 راهنمای استفاده از Docker

## ✅ وضعیت: آماده استفاده

Dockerfile و docker-compose.yml با موفقیت ساخته و تست شدند.

## 🚀 شروع سریع

```bash
cd test_project

# ساخت و اجرا
docker-compose up --build -d

# مشاهده لاگ‌ها
docker-compose logs -f

# توقف
docker-compose down
```

## 🌐 دسترسی

پس از اجرا، به آدرس زیر دسترسی دارید:

```
http://localhost:8001/
```

**نکته:** پورت 8001 استفاده شده است چون پورت 8000 توسط کانتینر دیگری استفاده می‌شود.

## 📋 ویژگی‌ها

- ✅ نصب خودکار وابستگی‌ها
- ✅ نصب کتابخانه از حالت توسعه
- ✅ اجرای خودکار migration در startup
- ✅ Volume mount برای تغییرات زنده
- ✅ آماده برای تست درگاه SEP

## ⚙️ تنظیمات

قبل از استفاده، کدهای درگاه را در `sep_test/settings.py` وارد کنید:

```python
"SEP": {
    "MERCHANT_CODE": "کد پذیرنده شما",
    "TERMINAL_CODE": "کد ترمینال شما",
},
```

## 🔧 دستورات مفید

```bash
# مشاهده لاگ‌ها
docker-compose logs -f

# دسترسی به shell
docker-compose exec web bash

# اجرای migration دستی
docker-compose exec web python manage.py migrate

# Restart
docker-compose restart

# توقف و حذف
docker-compose down
```

## 🎯 تست

1. به `http://localhost:8001/` بروید
2. روی "شروع تست پرداخت" کلیک کنید
3. مبلغ و شماره موبایل را وارد کنید
4. به درگاه بانک سامان هدایت می‌شوید

**موفق باشید! 🚀**




